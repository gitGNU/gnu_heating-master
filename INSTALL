Prerequesites:
  - GNU autotools are required
  - package 'pkg-config' is required
  - for more preqrequesites run './configure'

Building:
  -To build Heating Master, run './configure && make'

Installation:
  -To install Heating Master, run './configure && make && make install'
